#ifndef INSERTION_H
#define INSERTION_H


void insertionSort(int* ,int ,int);

int binarySearch(int*, int, int, int);
void insertionSortBinary(int*, int, int);

void shellSort(int*, int, int);
#endif
